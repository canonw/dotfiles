{{ if eq .chezmoi.os "darwin" -}}
{{ (joinPath .chezmoi.sourceDir ".config" "vscode" "User" "snippets" "shellscript.json" ) }}
{{ else -}}
{{   fail (printf "Unsupported OS for the client system: %s" .chezmoi.os) }}
{{ end -}}
