[include_app]
    [include_app.windows]
        aws_cli = true
        chezmoi = true
        clipboard_project = false # TODO: Integrate to windows
        curl = true
        direnv = false # No windows support
        git = true
        gopass = false # TODO: Integrate to windows
        lsd = false # TODO: Integrate to windows
        mintty = true
        ripgrep = true
        ssh = true
        wget = true
        xclip = false # Not use
        zoxide = true
