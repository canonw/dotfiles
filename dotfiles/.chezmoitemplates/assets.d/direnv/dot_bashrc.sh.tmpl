{{/* .bashrc script block */ -}}
{{/* {{ template "assets.d/direnv/dot_bashrc.sh.tmpl" . }} */ -}}
{{ template "assets.d/direnv/header.sh.tmpl" . }}
{{ if eq .chezmoi.os "linux" -}}
{{-  if eq .chezmoi.osRelease.id "ubuntu" -}}

test -f "$(command -v direnv)" && eval "$(direnv hook bash)"
{{-    if (.chezmoi.kernel.osrelease | lower | contains "microsoft") -}}
{{/* Nothing to setup in WSL */ -}}
{{-    else -}}
# TODO: Update setup
{{-    end -}}

{{ template "assets.d/direnv/aliases.sh.tmpl" . }}

{{-  else -}}
{{-  end -}}
{{- else if eq .chezmoi.os "darwin" -}}

test -f "$(command -v direnv)" && eval "$(direnv hook bash)"
{{ template "assets.d/direnv/aliases.sh.tmpl" . }}

{{- else if eq .chezmoi.os "windows" -}}
# TODO: Update setup
{{- else -}}
{{   fail (printf "Unsupported OS for the client system: %s" .chezmoi.os) }}
{{ end -}}
